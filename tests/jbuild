(jbuild_version 1)

(executables
  ((names (Test_parser Test_pratt_calc))
   (libraries (pratt proto fmt unix astring))
   (flags (:standard -open Proto))))

(alias
 ((name runtest)
  (package pratt)
  (deps (Test_parser.exe))
  (action (run ${<}))))

(alias
 ((name runtest)
  (package pratt)
  (deps (Test_pratt_calc.exe))
  (action (run ${<}))))

