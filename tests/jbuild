(jbuild_version 1)

(executable
  ((name Test_parser)
   (libraries (pratt pure fmt unix astring))
   (flags (:standard -open Pure -open Astring))))

(alias
 ((name runtest)
  (package pratt)
  (deps (Test_parser.exe))
  (action (run ${<}))))

